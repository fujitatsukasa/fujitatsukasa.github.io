name: Temporary Irodori legacy completion (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: ${{ false }}
    steps:
      - run: echo 'Superseded by temp_irodori_v4_fresh_matrix.yml'
